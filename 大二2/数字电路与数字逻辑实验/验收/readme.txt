该流水线CPU采用了暴力搜索算法进行字符串匹配，通过UART串口输入字符串，“！”作为换行符，前面为主字符串，后面为模式串
软件计算完成后，拉高SW0即可发送结果，结果为16进制，两位（也就是最多可以显示255次成功匹配，已经基本够用）
同时结果将显示在数码管上，不过由于已经实现了UART外设，按照老师要求不需要将结果显示在数码管上了，
所以这部分我没有完全按照要求实现，是利用软件控制数码管的开关切换，同时利用硬件映射结果使数码管显示特定数字实现的。
视频中进行了两次匹配运算，可以看到结果均正确。

